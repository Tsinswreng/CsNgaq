#import "@preview/tsinswreng-auto-heading:0.1.0": auto-heading
#let H = auto-heading
#let P(C) = {C}

\-\-\-

name: tsinswreng-proposal-clarity

description: 提出或解釋技術方案時，區分已知事實、建議新增、未驗證假設和待用戶決定事項，避免臆造現有能力、偷換需求和使用含糊術語

\-\-\-

#H[用途][
	涉及方案、設計、架構、流程或改動計劃時使用。
]

#H[標記事實][
	對每個關鍵內容標明：
	- #[*現有*：已被資料或使用者確認。]
	- #[*新增*：本方案建議加入。]
	- #[*假設*：尚未驗證。]
	- #[*待確認*：必須由使用者決定。]

	沒有證據時不得寫成現有能力。
]

#H[提案格式][
	先給結論，接著列出約束、要新增的內容、各場景的觸發與結果，最後列出真正待確認的問題。
	
	複雜流程用狀態表或時序表。
	每個操作說明觸發者、前置條件、狀態變化、失敗處理和完成條件。
]

#H[表達要求][
	- #[第一次出現的術語/標識符用白話定義。
		
		只有代碼中已有的,
		或上文已經定義的術語/標識符,
		在方案闡述中纔可以被引用。
		
		格式: 定義 `標識符/述語` 爲 ...
		
		例:
		假設用戶提問:
		````
		當前用戶登錄失敗時看不到具體原因, 怎麼區分?
		````
		
		錯誤回答示例:
		```
		如果是許可證過期就返回
		{
			"code": "LicenceExpired"
		}
		如果是 ...
		```

		正確示例:
		````
		新增異常錯誤碼。
		把不同異常情況分別定義成不同錯誤碼。
		定義 `LicenceExpired` 爲 許可證過期的錯誤碼;
		定義 ... 爲 ...
		````
	]
	
	不得把未確認的命令、字段、接口或流程說成已有，也不得自行補充使用者沒有要求的行為。
	發現前提衝突時先指出衝突，再列出選項和後果。
	不清楚現狀時寫明「未知」，不要猜。
]

#H[交付前自查][
	- 新增內容是否明確標為新增？
	- 修改內容是否明確說明?
	- 假設是否和事實分開？
	- 是否加入未授權的額外決定？
	- 讀者能否不靠猜測復述流程？
]
